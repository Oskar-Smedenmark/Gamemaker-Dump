///pointInDespawnRange(x,y)
/*
var xx = argument[0];
var yy = argument[1];

//var camD = point_distance(obj_camera.x, obj_camera.y, xx, yy);
var outX = xx < obj_camera.x - despawnRange || xx > obj_camera.x + despawnRange;
var outY = yy < obj_camera.y - despawnRange || yy > obj_camera.y + despawnRange;

var inRange = false;
if(outX || outY)
{
    inRange = true;
}

return inRange;*/
